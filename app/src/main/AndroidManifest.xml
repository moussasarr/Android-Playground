<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adder">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuDemo">
            <intent-filter>
                <action android:name="android.intent.action.MENUDEMO" />
            </intent-filter>

        </activity>
        <activity android:name=".Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN2ACTIVITY" />
            </intent-filter>
        </activity>
        <activity android:name=".webviewdemo">
            <intent-filter>
                <action android:name="android.intent.action.WEBVIEWDEMO" />
            </intent-filter>
        </activity>
        <activity
            android:name=".seekBar"
            android:label="@string/title_activity_seek_bar"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.SEEKBARR" />
            </intent-filter>
        </activity>
        <activity
            android:name=".signup"
            android:label="@string/title_activity_signup"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.SIGNUP" />
            </intent-filter>
        </activity>
        <activity android:name=".adding">
            <intent-filter>
                <action android:name="android.intent.action.ADDING" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>